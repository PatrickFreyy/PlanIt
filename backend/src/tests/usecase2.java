
Given(/^feature: starting the application$/, () => {
	return true;
});

(/^press the button to create an event$/, () => {
	return true;
});

Then(/^I see the window where I can insert the details of the event$/, () => {
	return true;
});

When(/^I insert all details$/, () => {
	return true;
});

When(/^I press the button create$/, () => {
	return true;
});

Then(/^The appointment is saved in the local storage$/, () => {
	return true;
});

Then(/^I can see the appointment in the schedule$/, () => {
	return true;
});

Given(/^I press the button to create an event$/, () => {
	return true;
});

Then(/^I see the window where I can insert the details of the event$/, () => {
	return true;
});

When(/^I insert some details$/, () => {
	return true;
});

When(/^I press the button create$/, () => {
	return true;
});

Then(/^I get a warning, that I have to fill all fields$/, () => {
	return true;
});

Then(/^the appointment is not saved in the local storage$/, () => {
	return true;
});
